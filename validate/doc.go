// Package validate defines immutable, strongly typed validators with structured
// errors, stable paths, aggregate and fail-fast calls, and typed localization.
// It can be used independently of the root shape package and never transforms.
package validate
