{
  "invalid_type": "{{if .Label}}{{.Label}}: {{end}}expected {{.Expected}}, received {{.Received}}",
  "required": "{{if .Label}}{{.Label}} is required{{else}}field is required{{end}}",
  "unknown_field": "field is not allowed",
  "too_small.string": "{{if .Label}}{{.Label}} {{end}}must contain at least {{.Expected}} characters",
  "too_big.string": "{{if .Label}}{{.Label}} {{end}}must contain at most {{.Expected}} characters",
  "string.len": "{{if .Label}}{{.Label}} {{end}}must contain exactly {{.Expected}} characters",
  "too_small.collection": "{{if .Label}}{{.Label}} {{end}}must contain at least {{.Expected}} items",
  "too_big.collection": "{{if .Label}}{{.Label}} {{end}}must contain at most {{.Expected}} items",
  "tuple.length": "{{if .Label}}{{.Label}} {{end}}must contain exactly the tuple length",
  "number.gt": "{{if .Label}}{{.Label}} {{end}}must be greater than {{.Expected}}",
  "number.gte": "{{if .Label}}{{.Label}} {{end}}must be greater than or equal to {{.Expected}}",
  "number.lt": "{{if .Label}}{{.Label}} {{end}}must be less than {{.Expected}}",
  "number.lte": "{{if .Label}}{{.Label}} {{end}}must be less than or equal to {{.Expected}}",
  "number.coerce": "{{if .Label}}{{.Label}}: {{end}}cannot be converted to {{.Expected}}",
  "number.finite": "{{if .Label}}{{.Label}} {{end}}must be a finite number",
  "number.finite_float64": "{{if .Label}}{{.Label}} {{end}}must be a finite float64 number",
  "number.json_integer": "{{if .Label}}{{.Label}} {{end}}must be an integer within the target type's range",
  "number.json_range": "{{if .Label}}{{.Label}}: {{end}}JSON number is outside the target type or is not integral",
  "number.convert": "{{if .Label}}{{.Label}}: {{end}}cannot be converted to the target number type",
  "invalid_enum": "{{if .Label}}{{.Label}} {{end}}must be one of the allowed values",
  "invalid_value.literal": "{{if .Label}}{{.Label}} {{end}}must equal the literal value",
  "invalid_value.bool_coerce": "{{if .Label}}{{.Label}}: {{end}}cannot be converted to bool",
  "invalid_value.duplicate_key": "key duplicates another parsed key",
  "invalid_value.unique_items": "{{if .Label}}{{.Label}} {{end}}must contain unique items",
  "invalid_value.unique_deep_limit": "too many items require deep uniqueness comparison",
  "invalid_format.pattern": "{{if .Label}}{{.Label}} {{end}}must match the required pattern",
  "invalid_format.time": "{{if .Label}}{{.Label}}: {{end}}cannot parse time using the configured layouts",
  "invalid_format.duration": "{{if .Label}}{{.Label}}: {{end}}cannot parse Go duration",
  "invalid_string.starts_with": "{{if .Label}}{{.Label}} {{end}}must start with {{printf \"%q\" .Expected}}",
  "invalid_string.ends_with": "{{if .Label}}{{.Label}} {{end}}must end with {{printf \"%q\" .Expected}}",
  "invalid_string.contains": "{{if .Label}}{{.Label}} {{end}}must contain {{printf \"%q\" .Expected}}",
  "invalid_email": "{{if .Label}}{{.Label}} {{end}}must be a valid email address",
  "invalid_url": "{{if .Label}}{{.Label}} {{end}}must be a valid absolute URL",
  "invalid_uuid": "{{if .Label}}{{.Label}} {{end}}must be a valid UUID",
  "invalid_ip": "{{if .Label}}{{.Label}} {{end}}must be a valid IP address",
  "invalid_union": "{{if .Label}}{{.Label}} {{end}}must match at least one schema",
  "invalid_union.oneof": "{{if .Label}}{{.Label}} {{end}}must match exactly one schema",
  "too_deep": "maximum recursive schema depth exceeded",
  "too_many_issues": "additional validation issues were omitted",
  "custom.validation_failed": "validation failed"
}
